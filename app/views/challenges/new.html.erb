<%= form_for(@challenge) do |f| %>
  
  <div class="field">
    <%= f.text_field :player_one, placeholder: "Compose new micropost..." %>
    <%= f.text_field :player_two, placeholder: "Compose new micropost..." %>
  </div>
  <%= f.submit "Create challenge", class: "btn btn-large btn-primary" %>
<% end %>